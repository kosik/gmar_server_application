# git sha-1 hash of the revision used to make this build
# buildNumber property is set by buildnumber-maven-plugin
#build.version=${buildNumber}
build.version=1

# date/time when this build was created
# buildTimestamp property is set in the root pom.xml
build.timestamp=${buildTimestamp}

# maven project version
release.version=${project.version}